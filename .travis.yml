language: java

stages:
  - build
  - test
  - deploy

jobs:
  include:
    - stage: build
      script:
	- chmod +x ./gradlew
        - (cd backend/; ./gradlew assemble)
    - stage: test
      script:
        - echo "Test"
    - stage: deploy
      script:
        - echo "Deployment"
